export enum EStatus {
    TODO = 'TODO',
    DOING = 'DOING',
    DONE = 'DONE',
}

export enum EPriority {
    HIGH = 'HIGH',
    MEDIUM = 'MEDIUM',
    LOW = 'LOW',
}

export enum EMarkType {
    HIGH = 'danger',
    MEDIUM = 'warning',
    LOW = 'success',
}
